apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vespa-metrics
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: vespa
  endpoints:
    - port: metrics
      interval: 30s
  namespaceSelector:
    matchNames:
      - default

